
public interface Configuration {
	
	// the width and height of game board
	static int boardWidth = 7;
	
	// the length of winning chain
	static int chainLength = 5;
	
	// the depth of MiniMax tree
	static int depth = 2;
	
	// the breadth of stochastic Agent
	static int breadth = (int) (boardWidth * boardWidth);
	
	static int X = 50;
	
	static int Y = 50;
	
	static int Size = 90;
	
	static int chessSize = 60;
}
